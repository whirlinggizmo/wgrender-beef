FileVersion = 1

# The binding, as a library project: examples depend on it (their BeefSpace.toml
# lists it at the repository root) and link wgrender themselves.
[Project]
Name = "wgr"
TargetType = "BeefLib"
